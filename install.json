{
  "run": [
    {
      "method": "shell.run",
      "params": {
        "venv": "env",
        "message": [
          "pip install -r requirements.txt"
        ]
      }
    },
    {
      "method": "fs.link",
      "params": {
        "venv": "env"
      }
    }
  ]
}